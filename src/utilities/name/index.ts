export const getFullName = (firstName: string, lastName: string) =>
  `${firstName} ${lastName}`;

export const getFullNameLength = (firstName: string, lastName: string) =>
  `${firstName} ${lastName}`.length;
